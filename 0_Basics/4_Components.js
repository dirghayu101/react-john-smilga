// Components are like functions and they have a return value.

function Greeting() {
  return <h2>Hello World!</h2>;
}

// Or
const Greeting = () => {
  return <h2>What's up baby?</h2>;
};

// Both of them are function.

// The return type that looks like HTML is technically called JSX.
